export class AppSettings {
   public static API_ENDPOINT: string = 'http://' + window.location.hostname + ':8080/api';
   public static WEBSOCKET_ENDPOINT: string = window.location.hostname + ':8081';
}